spring:
  application:
    name: jc-club-subject-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        namespace:
        server-addr: 122.152.204.49:8848
        file-extension: yaml
        prefix: ${spring.application.name}

      discovery:
        enabled: true
        server-addr: 122.152.204.49:8848

